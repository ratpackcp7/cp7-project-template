FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY src/ src/
COPY .env .env
RUN mkdir -p data
EXPOSE PROJECT_PORT
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "PROJECT_PORT"]
